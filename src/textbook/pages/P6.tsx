// 6장 — 입시 합격 예측 · 분류 문제의 첫 만남
import { Aside, Capture, CoreInsights, KeyTakeaways, LabBox, LabSteps, LabQuestions, LabTip, LearningGoals, MathCorner, M, Mb } from '../components';

export function TextbookP6() {
  return (
    <>
      <p className="lead text-lg leading-8 text-text/90">
        지금까지의 신경망은 <strong>"몇 점일까"</strong>를 맞히는 일이었다. 시험 점수를 예측하고,
        오차를 줄여 가며 직선을 데이터에 맞춰 갔다. 이번 장부터는 질문이 달라진다 —
        <strong> "합격일까, 불합격일까."</strong> 답이 두 갈래로 갈리는 문제다. 이런 문제를
        <strong> 분류(classification)</strong>라고 부른다. 회귀와는 풀이의 결이 조금 다르다.
      </p>
      <p>
        무대는 입시다. ○○대학교는 학생을 뽑을 때 국어·수학·영어·탐구 점수를 본다. 그런데
        어느 과목에 얼마나 가중치를 두는지는 비밀이다. 합격자와 불합격자의 점수만 보고, 우리는
        그 비밀 공식을 거꾸로 추적할 수 있을까. 페이즈 1~5에서 키운 단일 뉴런 한 개로 충분하다.
      </p>

      <LearningGoals
        items={[
          '회귀와 분류의 차이를 한 줄로 설명할 수 있다.',
          '결정 경계가 무엇인지, 가중치와 편향(합격컷)이 그 경계를 어떻게 정하는지 자기 말로 답할 수 있다.',
          '시뮬레이터에서 슬라이더를 움직여 학습 정확도를 80% 이상으로 끌어올릴 수 있다.',
          '학습 데이터에서 잘 맞아도 시험 데이터에서 떨어질 수 있는 이유를 설명할 수 있다.',
        ]}
      />

      <CoreInsights
        items={[
          '분류는 회귀와 다르다. "얼마나"가 아니라 "어느 쪽인가"를 묻는다. 출력은 결국 합격이냐 불합격이냐의 두 갈래로 모인다.',
          '결정 경계는 두 무리(합격/불합격)를 가르는 선이다. 가중치와 편향(합격컷)이 바로 그 선의 기울기와 위치를 정한다.',
          '가중치를 옮기면 결정 경계도 따라 회전·이동한다. 학습이란 결국 데이터를 가장 잘 가르는 결정 경계를 찾아내는 일이다.',
          '학습 데이터 10명만으로 그어 둔 경계는 새 학생 20명 앞에서 흔들릴 수 있다 — 이걸 일반화 문제라 한다.',
        ]}
      />

      <h2>6.1 회귀에서 분류로 — 질문이 달라진다</h2>
      <p>
        지금까지 풀던 문제와 한번 비교해 보자. 4장의 시험 점수 예측은 <strong>"몇 점일까"</strong>를
        맞히는 일이었다. 답이 0점부터 100점까지 연속으로 펼쳐진다. 이런 문제를 <strong>회귀(regression)</strong>
        라 부른다.
      </p>
      <p>
        그런데 입시는 다르다. 결과가 단 두 가지뿐이다 — <strong>합격</strong> 또는 <strong>불합격</strong>.
        50.3점 합격, 49.7점 불합격 같은 어중간한 답은 없다. 대학은 안과 밖, 두 무리로 학생을 가른다.
        이런 문제가 분류다. 운동 동아리에 들어갈지 말지, 친구의 게시물에 좋아요를 누를지 말지,
        스팸 메일인지 진짜 메일인지 — 우리 일상의 결정 대부분이 사실 분류다.
      </p>

      <Aside kind="key" title="회귀 vs 분류 한 줄 정리">
        ▸ <strong>회귀</strong> — "얼마나"를 묻는다. 답은 연속된 숫자(키, 점수, 가격).<br />
        ▸ <strong>분류</strong> — "어느 쪽인가"를 묻는다. 답은 정해진 몇 가지 중 하나(합격/불합격, 강아지/고양이).
      </Aside>

      <h2>6.2 단일 뉴런으로 합격 예측하기</h2>
      <p>
        놀랍게도 1장에서 본 단일 뉴런 한 개로 이 분류 문제를 풀 수 있다. 입력이 네 개로 늘었을 뿐
        구조는 똑같다. 학생 한 명의 점수를 <M>{`x_1, x_2, x_3, x_4`}</M> (국·수·영·탐)라고 하자.
        뉴런은 그 합산 점수를 이렇게 계산한다.
      </p>
      <Mb>{`s \\;=\\; w_1 x_1 + w_2 x_2 + w_3 x_3 + w_4 x_4`}</Mb>
      <p>
        여기에 마지막 한 단계가 더 붙는다. 합산 점수 <M>s</M>를 <strong>합격컷</strong> <M>c</M>와 비교한다.
        넘으면 합격, 같거나 작으면 불합격이다.
      </p>
      <Mb>{`\\hat y \\;=\\; \\begin{cases} 1\\ (\\text{합격}) & (s > c) \\\\ 0\\ (\\text{불합격}) & (s \\le c) \\end{cases}`}</Mb>
      <p>
        그런데 이 합격컷 <M>c</M>는 사실 1장에서 본 <strong>편향 <M>b</M>의 다른 얼굴</strong>이다.
        부등식의 양변에서 <M>c</M>를 빼서 한쪽으로 모아 보자.
      </p>
      <Mb>{`s > c \\;\\Longleftrightarrow\\; s - c > 0 \\;\\Longleftrightarrow\\; \\sum_i w_i x_i + \\underbrace{(-c)}_{=\\,b} > 0`}</Mb>

      <Aside kind="key" title="🔑 합격컷과 편향은 같은 것이다 — 부호만 반대">
        <p className="mb-2">
          <strong>합격컷 <M>c</M>를 5에서 6으로 올리는 것</strong>은 곧 <strong>편향 <M>b</M>를 −5에서 −6으로
          내리는 것</strong>과 똑같은 일이다. 둘은 같은 슬라이더의 두 가지 이름표일 뿐이다.
        </p>
        <Mb>{`b \\;=\\; -c`}</Mb>
        <p className="text-sm text-muted">
          왜 이게 중요할까? 다음 장부터는 합격컷이라는 단어 대신 <em>편향</em>으로 표기가 통일된다.
          그때 "합격컷이 사라졌네"라고 당황하지 말라는 뜻이다 — 부호만 뒤집힌 같은 친구다.
        </p>
      </Aside>

      <p>
        대학마다 합격컷이 다르다 — 어떤 대학은 7.0, 어떤 대학은 5.5. 이 숫자가 뉴런의 <strong>"기본 점수"</strong>를
        정한다.
      </p>

      <h2>6.3 결정 경계 — 두 무리를 가르는 선</h2>
      <p>
        합격자들과 불합격자들을 점수 그래프 위에 점으로 찍어 보자. 합격자는 위쪽 어딘가, 불합격자는
        아래쪽 어딘가에 모인다. 그 두 무리 사이를 갈라 주는 한 줄이 바로 <strong>결정 경계
        (decision boundary)</strong>다. 합격컷과 같은 식 <M>{`s = c`}</M>를 그래프로 그린 것이다.
      </p>
      <p>
        과목이 두 개(예: 수학, 영어)뿐이라면 결정 경계는 <em>한 줄짜리 직선</em>이 된다. 직선 위쪽에
        있으면 합격, 아래쪽이면 불합격. 가중치 <M>w</M>가 직선의 <strong>기울기</strong>를 정하고,
        편향(합격컷) <M>c</M>가 직선의 <strong>높낮이</strong>를 정한다. 슬라이더로 가중치를 움직이면
        그 선이 회전하고, 합격컷을 움직이면 그 선이 위아래로 미끄러진다.
      </p>
      <p>
        과목이 네 개로 늘어나면 결정 경계는 사람 눈으로 그릴 수 없는 4차원 공간 속의 평면이 된다.
        그래도 원리는 똑같다 — 점수를 넣어 합산하고, 한 숫자(합격컷)와 비교한다. 차원이 늘어도
        식 한 줄이라는 점은 변하지 않는다.
      </p>

      <Aside kind="note" title="잠깐 — 시그모이드는 어디로?">
        교과서에서는 흔히 합격 확률을 0~1 사이로 짜내기 위해 시그모이드 함수
        <M>{`\\sigma(z) = 1/(1+e^{-z})`}</M>를 끼워 넣는다. 이번 장의 시뮬레이터는 그 직전 단계,
        합산 점수와 합격컷을 직접 비교하는 가장 단순한 형태다. 시그모이드는 9장에서 다시 만난다.
      </Aside>

      <h2>6.4 학습 = 사람이 손으로 더듬는 경사하강법</h2>
      <p>
        5장에서는 컴퓨터가 자동으로 <M>{`w \\leftarrow w - \\eta\\, dw`}</M>로 가중치를 옮겨 손실을
        줄였다. 이번 장에서는 우리가 직접 슬라이더를 움직인다. 모든 가중치를 0.25로 두면 정답률이
        잘 안 오른다. 어떤 과목이 결정적인지 데이터를 보고 가설을 세운다 — "어쩌면 수학 점수가
        합격을 가르는 핵심일지도 모른다."
      </p>
      <p>
        그러고 나서 그 가중치를 조금 키워 본다. 정답률이 올라가면 그 방향이 내리막이다. 떨어지면
        반대로 돌린다. 이게 바로 <strong>경사하강법을 사람이 손으로 더듬는</strong> 일이다. 식 없이도
        같은 일을 할 수 있는 셈이다.
      </p>

      <h2>6.5 학습 데이터 vs 시험 데이터</h2>
      <p>
        시뮬레이터는 학생을 두 무리로 나눠 둔다. 학습용 10명과, 학습에는 한 번도 쓰지 않은 시험용
        20명이다. 학습 데이터에서 정답률이 100%가 나와도, 시험 데이터에서 60%밖에 안 나올 수 있다.
        왜일까. 우리가 본 10명의 패턴이 30명 전체의 패턴과 살짝 다를 수 있기 때문이다.
      </p>
      <p>
        진짜 실력은 <strong>시험 데이터</strong>에서 드러난다. 처음 보는 학생을 얼마나 잘 맞히는가 —
        이걸 <strong>일반화 능력</strong>이라 한다. 다음 7장에서 자세히 다룬다.
      </p>

      <LabBox step="실습 6-1" title="비밀 공식 추적 — 시뮬레이터에서 가중치 조정">
        <p>
          페이지 상단의 <strong>"▶ 실습 앱에서 직접 만져보기"</strong>를 누른다. 시나리오를 두 개 중
          하나 고른다 — 정시(국·수·영·탐) 또는 학종(내신·자소서·면접·활동). 어느 쪽이든 풀이의 흐름은
          같다. 탭이 세 개로 나뉘어 있다.
        </p>

        <Capture
          src="/textbook/captures/p6.png"
          alt="페이즈 6 입시 합격 예측 시뮬레이터 화면"
          caption="▲ 가중치 4개와 합격컷을 슬라이더로 움직이며 학습 데이터 10명의 정답률을 끌어올린다. 학생 카드의 색이 실시간으로 바뀐다(초록=맞음, 빨강=틀림)."
        />

        <LabSteps>
          <li>탭 <strong>① 상황과 데이터</strong>에서 학생 10명의 점수 표를 천천히 본다. 합격자들이 유난히 높은 항목이 있는지 눈으로 찾아본다.</li>
          <li>탭 <strong>② 가중치 조정</strong>으로 넘어가 모든 슬라이더를 0.25로 둔 채 정답률을 본다. 균등 가중치는 보통 답이 아니다.</li>
          <li>가설을 세운 항목의 가중치를 0.4~0.5까지 키워 본다. 동시에 다른 항목의 가중치는 0.1~0.2로 낮춘다. 정답률이 오르면 방향이 맞았다는 신호다.</li>
          <li>합격컷 슬라이더를 4 → 7 사이로 움직여 본다. 컷이 너무 낮으면 모두를 합격으로 보내고, 너무 높으면 모두를 불합격으로 보낸다. 사이의 황금 지점을 찾는다.</li>
          <li>학습 정답률 8/10 (80%) 이상이 되면 탭 <strong>③ 시험 데이터로 평가</strong>로 넘어간다. 새 학생 20명에서 몇 점이 나오는지 본다.</li>
        </LabSteps>

        <LabQuestions>
          <li>모든 가중치를 0.25로 두면 어떤 학생도 잘 분류되지 않는다. 왜 균등 가중치는 답이 될 수 없을까?</li>
          <li>학습 정답률이 100%인데 시험 정답률이 70%라면, 우리 모델은 어떤 함정에 빠진 걸까?</li>
          <li>합격컷을 매우 높게(예: 8.0) 두면 모든 학생이 불합격이 된다. 그런데도 시뮬레이터의 정답률이 0이 아닐 수 있다. 왜 그럴까? (힌트: 실제 불합격자도 표 안에 있다.)</li>
        </LabQuestions>

        <LabTip>
          학생 카드 아래에 <code>5.20 ≤ 7.0 → 불</code> 같은 식이 실시간으로 뜬다. 슬라이더를
          움직이면서 그 숫자가 어떻게 따라오는지를 눈으로 좇으면 머리에 남는다. 식을 외우는 것보다
          한 번 손으로 움직여 보는 것이 빠르다.
        </LabTip>
      </LabBox>

      <MathCorner title="결정 경계의 식 — 두 과목 그래프 위에 직선 한 줄">
        <p>
          과목을 단 두 개(수학 <M>{`x_1`}</M>, 영어 <M>{`x_2`}</M>)로 줄여 보자. 합격 조건은 다음과 같다.
        </p>
        <Mb>{`w_1 x_1 + w_2 x_2 > c`}</Mb>
        <p>
          여기서 등호가 성립하는 순간, 즉 <M>{`w_1 x_1 + w_2 x_2 = c`}</M>가 결정 경계다. 이 식을
          중3 때 배운 일차함수 모양으로 풀어 보자.
        </p>
        <Mb>{`x_2 \\;=\\; -\\frac{w_1}{w_2}\\, x_1 + \\frac{c}{w_2}`}</Mb>
        <p>
          깔끔한 직선 <M>{`y = ax + b`}</M> 모양이다. 기울기는 <M>{`-w_1/w_2`}</M>, y절편은 <M>{`c/w_2`}</M>.
          가중치 두 개의 비율이 직선의 기울기를 정하고, 합격컷이 직선의 위치를 정한다.
        </p>
        <p>
          그러니 슬라이더로 <M>{`w_1`}</M>을 키우면 결정 경계가 회전하고, 합격컷 <M>c</M>를
          키우면 결정 경계가 위로 미끄러진다. <strong>학습이란 결국 이 직선의 기울기와 위치를
          데이터에 맞춰 조정하는 일</strong>이다.
        </p>
      </MathCorner>

      <MathCorner title="시그모이드 — 합격 확률을 0~1 사이로 짜내기">
        <p>
          이 장의 시뮬레이터는 <em>합격이냐 불합격이냐</em>를 0/1로 단번에 가른다. 그런데 실제
          입시는 더 부드럽다 — "합격 확률 87%"처럼 확률로 말한다. 합산 점수 <M>z</M>를 0~1 사이의
          확률로 짜내려면 <strong>시그모이드 함수</strong>를 통과시킨다.
        </p>
        <Mb>{`\\sigma(z) \\;=\\; \\frac{1}{1 + e^{-z}}`}</Mb>
        <p>
          <M>z</M>가 매우 크면 <M>{`\\sigma(z) \\to 1`}</M>(거의 합격), 매우 작으면 <M>{`\\sigma(z) \\to 0`}</M>(거의
          불합격). 0과 그 사이의 모든 값이 부드러운 S자 곡선으로 이어진다. 이름이 어렵게 들려도
          <strong>"점수를 0~1 사이로 압축하는 함수"</strong>일 뿐이다. 9장에서 다시 자세히 다룬다.
        </p>
      </MathCorner>

      <KeyTakeaways
        items={[
          '회귀는 "얼마나"를 묻고, 분류는 "어느 쪽인가"를 묻는다. 입시 합격 예측은 분류 문제다.',
          '단일 뉴런 한 개로 분류 문제를 풀 수 있다 — 가중치 4개와 합격컷(편향) 1개가 비밀의 전부다.',
          '결정 경계는 두 무리를 가르는 선이다. 가중치가 그 선의 기울기를, 편향이 위치를 정한다.',
          '학습 데이터에서 잘 맞아도 시험 데이터에서 떨어질 수 있다 — 이걸 일반화 문제라 한다. 다음 장에서 데이터를 더 모아 본다.',
        ]}
      />
    </>
  );
}
