// 4장 — 학습률, 보폭의 크기 (고1 친화 톤)
import { Aside, Capture, CoreInsights, KeyTakeaways, LabBox, LabSteps, LabQuestions, LabTip, LearningGoals, MathCorner, M, Mb, TrainingCycleStrip } from '../components';

export function TextbookP4() {
  return (
    <>
      <p className="lead text-lg leading-8 text-text/90">
        3장에서 우리는 손실이라는 산을 만났다. <strong>경사하강법</strong>이라는 작전도 배웠다 — 곡선의 기울기를 보고
        그 반대 방향으로 한 발씩 내려가는 것. 그런데 한 가지가 비어 있다. <strong>한 발이 얼마나 큰 발이냐</strong>는 것이다.
        그 보폭의 크기를 정하는 한 글자가 바로 이 장의 주인공, <strong>학습률 η(에타)</strong>다.
      </p>
      <p>
        학습률은 식에 단 한 번 등장하는 작은 글자다. 그런데 이 한 글자를 잘못 정하면 산을 영원히 못 내려온다.
        너무 크게 잡으면 정답을 폴짝 뛰어넘어 양옆으로 튀고, 너무 작게 잡으면 한 발 한 발이 너무 답답하다.
        가중치가 학습의 "결과"라면, 학습률은 학습의 "성패"를 좌우한다.
      </p>

      <LearningGoals
        items={[
          '학습률 η가 갱신 식 w ← w − η · (기울기)의 어디에 들어가는지, 왜 거기에 곱해지는지를 한 줄로 설명할 수 있다.',
          '학습률이 너무 크거나(발산·진동), 너무 작을 때(느린 학습) 학습 곡선이 어떤 모양이 되는지 그림으로 그려 낼 수 있다.',
          '시뮬레이터에서 같은 곡선·같은 출발점·같은 식을 두고 η만 바꿔 가며, 보폭 하나가 결과를 어떻게 바꾸는지를 비교 체험할 수 있다.',
        ]}
      />

      <CoreInsights
        items={[
          '학습률 η는 한 step의 "보폭"이다. 갱신 식 w ← w − η · dL/dw에서 기울기 앞에 곱해지는 한 글자가 그 보폭의 크기를 정한다.',
          '보폭이 너무 크면 정답을 지나쳐 양옆으로 진동하다 결국 발산한다. 너무 작으면 답답하리만치 느리다. 학습의 성패는 가중치 자체보다 학습률에 더 크게 좌우된다.',
          '"딱 맞는 학습률"은 데이터·모델마다 다르다. 그래서 실제 딥러닝에서 학습률 튜닝(고르기)은 가장 중요한 작업 중 하나다.',
        ]}
      />

      <TrainingCycleStrip
        active={[5]}
        caption="이 장은 ⑤만 본다. 보폭의 크기 η — 앞 장에서 자동으로 결정된 ④ 기울기에 곱해 주는 한 글자."
      />

      <h2>4.1 한 글자가 한 발의 크기를 정한다</h2>
      <p>
        3장에서 우리는 손실 곡선 위 한 점을 잡고, 그 점의 기울기 반대 방향으로 가중치를 한 발 옮겼다.
        그 한 줄짜리 식을 다시 적어 보자.
      </p>
      <Mb>{`w \\;\\leftarrow\\; w \\;-\\; \\eta \\cdot \\dfrac{\\partial L}{\\partial w}`}</Mb>
      <p>
        오른쪽 항의 <M>{`\\partial L / \\partial w`}</M>는 곡선이 결정한다. 우리가 정할 수 없다. 데이터가 정해지고 모델이
        정해지면 그 점에서의 기울기는 자동으로 정해진다. <strong>우리가 손댈 수 있는 건 오직 그 앞에 곱해진 η 한 글자</strong>다.
      </p>
      <p>
        η가 0.1이라면, 한 step에 기울기의 10%만큼 움직인다. η가 1.0이라면 기울기 그대로 움직인다.
        η가 0.01이라면 100분의 1만큼만 움직인다. <strong>걷는 보폭의 크기를 곱해 주는 한 글자</strong>다.
      </p>

      <Aside kind="key" title="이름의 의미 — η는 그저 그리스 글자 하나">
        η는 그리스 문자 "에타(eta)"다. 영어로는 learning rate. 무서운 기호처럼 보이지만, 그냥 학습률을
        부를 때 쓰는 약속된 한 글자일 뿐이다. 본문에서는 η와 "학습률"을 같은 뜻으로 섞어 쓴다.
      </Aside>

      <h2>4.2 너무 크면 — 산을 뛰어넘는다</h2>
      <p>
        보폭이 너무 크면 어떻게 될까. 산을 내려가는 사람을 떠올려 보자. 아래쪽으로 폭이 좁은 골짜기가 있고,
        그 바닥이 정답이다. 그런데 보폭이 골짜기 폭보다 더 크다면? 한 발에 골짜기를 그대로 뛰어넘어 반대편 비탈에
        올라서게 된다. 그 자리에서 다시 기울기를 보고 한 발 내려가면? 또 골짜기를 뛰어넘어 원래 비탈로 돌아온다.
        이렇게 계속 양옆으로 왔다 갔다 하는 모양이 <strong>진동(oscillation)</strong>이다.
      </p>
      <p>
        보폭이 더 커지면 진동의 폭이 step마다 커진다. 결국 손실 곡선 바깥으로 튕겨 나가 버린다. 이걸
        <strong>발산(divergence)</strong>이라 부른다. 학습은커녕 점이 화면 밖으로 사라진다.
      </p>

      <h2>4.3 너무 작으면 — 답답하리만치 느리다</h2>
      <p>
        반대로 보폭이 너무 작으면? 안전하다. 정답을 지나칠 일도 없다. 다만 한 발 한 발이 너무 작아서
        같은 정답에 도달하는 데 step이 훨씬 많이 필요하다. 1000 step에 끝날 학습이 100,000 step이 걸리기도 한다.
        시간이 그만큼 더 든다. 컴퓨터 자원은 그만큼 더 든다.
      </p>
      <p>
        "그럼 그냥 안전하게 작게 잡으면 되는 거 아니냐"고 생각할 수 있다. 그러나 실제 딥러닝 모델은
        한 번 학습에 며칠씩 걸린다. 100배 느린 학습률은 100배의 전기와 시간을 먹는다. 게다가 학습률이 너무
        작으면, 곡선의 작은 굴곡(작은 골짜기)에 걸려 빠져나오지 못하기도 한다. 너무 작은 것도 결국 답이 아니다.
      </p>

      <h2>4.4 갱신 식에 η가 들어가는 이유</h2>
      <p>
        다시 갱신 식을 보자. <M>{`w \\leftarrow w - \\eta \\cdot \\partial L / \\partial w`}</M>. η가 왜 하필 기울기 앞에 곱해질까.
      </p>
      <p>
        기울기 <M>{`\\partial L / \\partial w`}</M>는 "방향과 크기"를 동시에 가진다. 부호는 어느 쪽으로 가야 손실이 줄어드는지를,
        절댓값은 그 점의 비탈이 얼마나 가파른지를 알려 준다. 비탈이 가파를수록 같은 한 step도 더 멀리 움직이게 된다.
      </p>
      <p>
        그런데 그 "비탈의 가파름"을 그대로 보폭으로 쓰면 위험하다. 비탈이 너무 가파른 자리에서는 한 step에 너무 멀리
        튀어 나간다. 그래서 그 앞에 작은 양수 η를 곱해 <strong>전체적으로 보폭을 줄여 주는</strong> 것이다.
        η는 일종의 <em>"브레이크"</em>인 셈이다. 비탈의 정보(방향과 크기)는 그대로 살리되, 한 step에 가는 거리만 사람이 정한다.
      </p>

      <Aside kind="note" title="학습률은 가중치보다 더 중요할 때가 많다">
        가중치 <M>w</M>는 학습이 알아서 찾아 준다. 그러나 학습률 η는 사람이 직접 정해야 하는 값이다. 이렇게 사람이 정해 주는
        값을 <strong>하이퍼파라미터(hyperparameter)</strong>라 한다. 좋은 학습률을 고르는 일은 좋은 모델을 고르는 일만큼,
        때로는 그보다 더 중요하다.
      </Aside>

      <h2>4.5 같은 곡선, 다른 보폭 — 한 화면 비교</h2>
      <p>
        말로는 "너무 크면 진동, 너무 작으면 느림"이라 적었다. 그러나 머릿속에 그림이 그려지지 않는다. 다음 실습에서는
        <strong> 같은 손실 곡선·같은 출발점·같은 식</strong>을 두고 학습률만 네 가지로 다르게 줘서, 같은 자리가
        얼마나 다르게 굴러가는지를 한 화면에 담아 본다.
      </p>

      <LabBox step="실습 4-1" title="네 가지 보폭으로 같은 곡선 굴리기">
        <p>
          페이지 상단의 <strong>"▶ 실습 앱에서 직접 만져보기"</strong> 버튼으로 인앱 시뮬레이터를 연다.
          3장에서 본 그 다섯 점 손실 곡선이 그대로 있고, 그 위로 네 개의 점이 각자 자기 보폭만큼 굴러내려간다.
        </p>

        <Capture
          src="/textbook/captures/p4.png"
          alt="페이즈 4 학습률 비교 시뮬레이터 화면"
          caption="▲ 같은 손실 곡선 위에서 η = 1.2(빨강) · 1.0(주황) · 0.1(초록) · 0.01(파랑)이 동시에 굴러내려간다. 출발점은 모두 b = −2로 같다. 정답은 b = 0.7."
        />

        <LabSteps>
          <li><strong>+1 step</strong>을 한 번씩 천천히 눌러 본다. 한 step마다 네 점이 어디로 옮겨 가는지 자기 눈으로 따라간다.</li>
          <li>몇 step 진행한 뒤, <strong>η = 1.2</strong> 점(빨강)이 어디 있는지 본다. 진폭이 step마다 커지며 곡선 밖으로 튕겨 나가는 모습이 보일 것이다.</li>
          <li><strong>η = 1.0</strong> 점(주황)은 정답을 정확히 지나쳤다 다시 원위치로 돌아오는 왕복을 반복한다. "딱 정답을 못 잡고 영원히 진동하는" 경계의 모양이다.</li>
          <li><strong>η = 0.1</strong> 점(초록)은 매 step마다 손실이 줄어 빠르게 정답에 도달한다 — "딱 맞는" 보폭이다.</li>
          <li><strong>η = 0.01</strong> 점(파랑)은 같은 방향으로 가지만 한 발이 너무 작아 답답하게 느리다. <strong>+50</strong>을 눌러 step을 많이 진행해 봐도, 같은 정답에 도달하는 데 훨씬 오래 걸린다.</li>
        </LabSteps>

        <LabQuestions>
          <li>η = 1.2와 η = 1.0의 차이는 0.2뿐이다. 그런데 결과는 "발산"과 "진동"으로 완전히 다르다. 왜 이렇게 작은 차이가 큰 결과 차이를 만들까?</li>
          <li>η = 0.01도 결국 정답에 도달한다. 그런데 왜 이 학습률은 "안전하지만 답답하다"고 평가받을까? 어떤 상황에서 그 답답함이 큰 문제가 될까?</li>
          <li>이 실습에서는 모두 같은 출발점(b = −2)에서 시작했다. 만약 한 점이 정답에 더 가까운 자리(예: b = 0.5)에서 출발했다면, "딱 맞는 학습률"의 값도 똑같을까? 자기 말로 짐작해 보자.</li>
        </LabQuestions>

        <LabTip>
          이 실습 곡선은 안정 조건이 <code>0 &lt; η &lt; 1</code>이다(곡률이 일정해서 그렇다). 그래서 η = 1.0이 딱 진동의 경계가 된다.
          현실의 딥러닝 손실 곡선은 자리마다 곡률이 다르니, "딱 맞는 보폭"도 자리마다 다르게 잡아야 한다.
        </LabTip>
      </LabBox>

      <MathCorner title="학습률 스케줄링 — 처음엔 크게, 나중엔 작게">
        <p>
          한 가지 자연스러운 아이디어가 있다. <strong>"처음에는 보폭을 크게 잡아 빨리 내려가고, 정답에 가까워지면 보폭을 줄여
          미세 조정하면 어떨까?"</strong> 산을 내려가는 사람도 처음엔 성큼성큼 가다, 골짜기 바닥 근처에서는 살금살금 걷는다.
        </p>
        <p>
          이걸 식으로 표현한 것이 <strong>학습률 스케줄링(learning rate scheduling)</strong>이다. 가장 간단한 형태는
          <em> 지수 감소(exponential decay)</em>다. step <M>t</M>마다 학습률에 1보다 약간 작은 수를 곱해 조금씩 줄인다.
        </p>
        <Mb>{`\\eta_{\\,t} \\;=\\; \\eta_{\\,0} \\cdot \\gamma^{\\,t}`}</Mb>
        <p>
          여기서 <M>{`\\eta_0`}</M>는 처음 학습률, <M>{`\\gamma`}</M>(감마)는 0.99처럼 1보다 살짝 작은 수다.
          예를 들어 <M>{`\\eta_0 = 0.1`}</M>, <M>{`\\gamma = 0.99`}</M>면 100 step 뒤 학습률은 약 <M>{`0.1 \\times 0.99^{100} \\approx 0.037`}</M>로
          처음의 절반 이하로 줄어든다.
        </p>
        <p className="text-sm text-muted">
          현실에서는 이외에도 "특정 step마다 절반으로 줄이기", "코사인 곡선 모양으로 부드럽게 줄이기" 같은 다양한 스케줄이
          쓰인다. 공통점은 같다 — <strong>처음엔 크게, 나중엔 작게</strong>.
        </p>
      </MathCorner>

      <KeyTakeaways
        items={[
          '학습률 η는 갱신 식 w ← w − η · (기울기)의 기울기 앞에 곱해지는 한 글자다. 한 step의 보폭을 정한다.',
          '너무 크면 정답을 지나쳐 진동하다 발산한다. 너무 작으면 안전하지만 답답하리만치 느리다.',
          'η는 학습이 찾아 주는 값이 아니라 사람이 정해 주는 하이퍼파라미터다. 좋은 학습률을 고르는 일이 학습의 성패를 가른다.',
          '실제로는 "처음엔 크게, 나중엔 작게" 줄여 가는 학습률 스케줄링도 자주 쓴다.',
        ]}
      />
    </>
  );
}
