// 1장 — 인공 뉴런 해부 (고1 친화 톤)
import { Aside, Capture, CoreInsights, KeyTakeaways, LabBox, LabSteps, LabQuestions, LabTip, LearningGoals, MathCorner, M, Mb } from '../components';

export function TextbookP1() {
  return (
    <>
      <p className="lead text-lg leading-8 text-text/90">
        인공 신경망(AI)은 어떻게 생긴 걸까. 생각보다 단순하다. <strong>아주 작은 계산기 한 개</strong>를 잔뜩
        이어 붙인 것이다. 이 작은 계산기 한 개를 <strong>인공 뉴런</strong>이라 부른다. 그러니
        신경망을 알려면 먼저 이 작은 계산기 한 개부터 풀어 헤쳐 보면 된다.
      </p>
      <p>
        이 장에서는 인공 뉴런 한 개를 분해한다. 부품은 딱 세 개다 — <strong>가중치 · 편향 · 활성화 함수</strong>.
        이름은 어렵게 들려도, 하나씩 보면 모두 우리가 이미 알고 있는 것들이다.
      </p>

      <LearningGoals
        items={[
          '인공 뉴런의 세 부품(가중치 · 편향 · 활성화 함수)을 자기 말로 설명할 수 있다.',
          '입력값과 가중치·편향이 주어졌을 때 뉴런의 출력 y를 직접 손으로 계산할 수 있다.',
          'ReLU가 왜 필요한지, 활성화 함수가 빠지면 어떤 점이 불편한지 한 줄로 답할 수 있다.',
        ]}
      />

      <CoreInsights
        items={[
          '인공 뉴런은 결국 "점수 계산기" 한 개다. 입력에 가중치를 곱해 모두 더하고, 거기에 편향을 보탠 뒤, 활성화 함수가 그 합을 통과시킬지 막을지 정한다.',
          '가중치는 그 입력의 영향력을 정하는 수, 편향은 입력과 상관없이 항상 더해지는 기본 점수, 활성화 함수는 합을 신호로 바꿔 줄지 결정하는 문지기다.',
          '활성화 함수 ReLU는 입력 공간을 한 번 접는 종이접기다. 음수 영역은 0의 바닥으로 접혀 들어가고, 양수 영역만 살아남는다. 이 접기를 여러 뉴런이 겹치면, 평평했던 종이가 복잡한 모양으로 빚어진다.',
        ]}
      />

      <h2>1.1 인공 뉴런은 점수 계산기다</h2>
      <p>
        한 가지 상황을 떠올리자. 친구의 인스타 게시물에 내가 좋아요를 누를지 말지를 예측하는 작은
        프로그램을 만든다고 하자. 입력으로 두 가지를 본다.
      </p>
      <ul className="list-disc pl-6 my-3 leading-7">
        <li><M>{`x_1`}</M> = 그 친구가 일주일에 올린 게시물 수</li>
        <li><M>{`x_2`}</M> = 사진의 화질 (1~5점)</li>
      </ul>
      <p>
        인공 뉴런은 이 두 값을 받아 <strong>"좋아요를 누를 점수"</strong>를 계산한다.
        절차는 세 단계뿐이다.
      </p>
      <ol className="list-decimal pl-6 my-3 leading-7 space-y-1">
        <li>두 입력에 각각 <strong>중요도</strong>(가중치)를 곱한다.</li>
        <li>곱한 값을 모두 더하고, 거기에 <strong>기본 점수</strong>(편향)를 보탠다.</li>
        <li>그 합이 일정 수준을 넘으면 신호를 내보낸다(활성화 함수).</li>
      </ol>
      <p>
        끝이다. 인공 뉴런 한 개는 이게 전부다. 이제 세 부품을 하나씩 따로 보자.
      </p>

      <Aside kind="key" title="이 장의 결론을 미리">
        인공 뉴런 한 개는 결국 한 줄짜리 식이다.
        <Mb>{`y = f(\\,w_1 x_1 + w_2 x_2 + b\\,)`}</Mb>
        세 글자(<M>w</M>, <M>b</M>, <M>f</M>)가 뉴런의 비밀 전부다.
      </Aside>

      <h2>1.2 가중치 — "이 입력이 얼마나 중요한가"</h2>
      <p>
        가중치(<M>w</M>)는 <strong>그 입력을 얼마나 중요하게 볼 것인가</strong>를 정하는 숫자다.
        영어로는 weight, 말 그대로 무게다.
      </p>
      <p>
        예를 들어 보자. 게시물 수의 가중치 <M>{`w_1`}</M>를 크게, 화질의 가중치 <M>{`w_2`}</M>를
        작게 두면 어떻게 될까. 이 뉴런은 "자주 올리는 친구를 좋아할 사람"이라 보는 셈이다.
        반대로 가중치를 뒤집으면 "사진이 예뻐야 좋아하는 사람"으로 본다. 같은 식이지만 가중치만
        다르면 판단이 달라진다.
      </p>
      <p>
        가중치가 0이면 그 입력은 무시된다. 가중치가 음수면 입력이 늘수록 점수가 오히려 줄어든다.
        예를 들어 "사진이 너무 화려하면 오히려 안 누른다"는 사람도 있을 수 있다 — 그런 사람의
        가중치 <M>{`w_2`}</M>는 음수다.
      </p>

      <h2>1.3 편향 — "기본 점수"</h2>
      <p>
        편향(<M>b</M>)은 입력과 상관없이 <strong>항상 더해지는 기본 점수</strong>다.
        가중치가 직선의 기울기를 정한다면, 편향은 그 직선이 어디서 시작하는지를 정한다.
      </p>
      <p>
        편향이 크면 그 사람은 "웬만하면 좋아요를 누르는 사람"이 된다. 편향이 작거나 음수면
        "어지간해서는 안 누르는 깐깐한 사람"이 된다.
      </p>
      <p>
        편향이 왜 필요한지는 이미 배운 일차함수로 설명할 수 있다. <M>{`y = wx`}</M>는 항상
        원점을 지나야 하는 직선이다. <M>{`y = wx + b`}</M>가 되어야 비로소 직선이 그래프 위
        어디서든 시작할 수 있다. 편향이 있어야 뉴런도 자유로워진다.
      </p>

      <h2>1.4 활성화 함수 — "신호를 낼지 말지 정하는 문지기"</h2>
      <p>
        가중치와 편향으로 만든 합을 <M>z</M>라고 부른다.
      </p>
      <Mb>{`z = w_1 x_1 + w_2 x_2 + b`}</Mb>
      <p>
        그런데 <M>z</M>를 그대로 답으로 내보내면 인공 뉴런은 결국 일차함수일 뿐이다.
        일차함수만 가지고는 곡선처럼 복잡한 패턴을 절대 못 그린다. 그래서 마지막에 한 번 더
        손을 봐 준다. 이 마지막 손질이 <strong>활성화 함수</strong>(activation function, <M>f</M>)다.
      </p>

      <p>
        가장 단순한 활성화 함수가 <strong>ReLU</strong>(렐루)다. 정의는 한 줄이다.
      </p>
      <Mb>{`\\mathrm{ReLU}(z) = \\max(0,\\, z)`}</Mb>
      <p>
        말로 풀면 이렇다 — <strong>합이 음수면 그냥 0으로 막아 버리고, 양수면 그대로 통과시킨다</strong>.
        뉴런이 신호를 낼지 말지를 정하는 문지기다. 점수가 음수까지 떨어지는 순간 "오늘은 안 누를래"
        하고 입을 다무는 것이다.
      </p>
      <p>
        이 단순한 "꺾임"이 신경망의 비밀이다. 일차함수에 한 번 꺾임을 넣어 주는 것만으로도 뉴런은
        직선만으로는 풀 수 없는 문제를 풀게 된다. 자세한 이유는 11장에서 다시 본다.
      </p>

      <Aside kind="note" title="활성화 함수가 ReLU만 있는 건 아니다">
        시그모이드, tanh, 소프트맥스 등 종류는 많다. 각각 쓰는 자리가 다르다. 이 책에서는
        1~4부 내내 가장 쉬운 ReLU만 쓴다. 이름만 알아 두면 된다.
      </Aside>

      <h2>1.5 식 한 줄로 다시 보기</h2>
      <p>
        세 부품을 한 줄로 모으면 인공 뉴런은 이렇게 적힌다.
      </p>
      <Mb>{`y = f(w_1 x_1 + w_2 x_2 + \\dots + w_n x_n + b)`}</Mb>
      <p>
        입력이 두 개든 천 개든 식의 모양은 똑같다. 곱셈 항만 늘어날 뿐이다.
        실제 신경망에서는 입력이 보통 수백~수만 개에 이른다. 그래도 한 줄짜리 식이라는 건 변함없다.
      </p>

      <LabBox step="실습 1-1" title="시뮬레이터에서 세 부품 직접 움직여 보기">
        <p>
          페이지 상단의 <strong>"▶ 실습 앱에서 직접 만져보기"</strong> 버튼을 눌러 인앱 시뮬레이터를
          연다. 입력 두 개와 그것을 받는 뉴런이 그림으로 표시되고, 슬라이더로 가중치·편향을 직접
          움직일 수 있다.
        </p>

        <Capture
          src="/textbook/captures/p1.png"
          alt="페이즈 1 인공 뉴런 시뮬레이터 화면"
          caption="▲ 입력 x₁=3, x₂=2가 가중치를 거쳐 합쳐지고, ReLU를 통과해 y가 나온다. 화면 위쪽에 1-1(가중치) / 1-2(편향) / 1-3(활성화) 세 탭이 있다."
        />

        <LabSteps>
          <li>탭 <strong>1-1 가중치</strong>에서 <M>{`w_1`}</M>을 0 → 2 → −2로 움직여 본다. <M>y</M>가 어떻게 따라오는지, 그림 속 선의 굵기·방향이 어떻게 바뀌는지 본다.</li>
          <li>탭 <strong>1-2 편향</strong>에서 <M>b</M>만 −5 → 0 → +5로 움직여 본다. 가중치는 그대로 둔 채 편향만 바꿔도 출력이 따라온다는 것을 확인한다.</li>
          <li>탭 <strong>1-3 활성화 함수</strong>에서 슬라이더를 음수까지 끌어내려 본다. 출력이 0에서 멈춰 버리는 순간을 직접 본다.</li>
        </LabSteps>

        <LabQuestions>
          <li><M>{`w_1`}</M>이 −1일 때, 게시물 수가 늘어나면 좋아요 점수는 어떻게 될까? 한 줄로 자기 말로 적어 보자.</li>
          <li>편향 <M>b</M>를 +10처럼 매우 크게 두면, 어떤 입력이 들어와도 출력이 0이 되지 않는다. 왜 그럴까?</li>
          <li>활성화 함수를 빼고 <M>{`y = z`}</M>를 그대로 쓰면 어떤 점이 불편할까? (힌트: 일차함수로 곡선을 그릴 수 있을까?)</li>
        </LabQuestions>

        <LabTip>
          시뮬레이터 아래쪽 "계산 과정" 박스에 식이 단계별로 나온다. 슬라이더를 움직이면서 그 식의
          숫자만 따라 읽어도 머리에 남는다. 일단 손이 먼저 익혀야 머리가 따라온다.
        </LabTip>
      </LabBox>

      <MathCorner title="합산 기호 ∑로 짧게 적기">
        <p>
          입력이 둘일 때는 <M>{`w_1 x_1 + w_2 x_2`}</M>처럼 손으로 다 적을 수 있다. 그런데 입력이
          1000개라면? 다 적기 너무 길다. 이럴 때 쓰는 게 <strong>시그마(∑)</strong>다.
        </p>
        <p>
          이 기호는 <em>"같은 모양의 항을 인덱스만 바꿔가며 모두 더해라"</em>는 약속일 뿐이다.
          어려운 것이 아니다.
        </p>
        <Mb>{`\\sum_{i=1}^{n} w_i x_i \\;=\\; w_1 x_1 + w_2 x_2 + \\dots + w_n x_n`}</Mb>
        <p>
          기호 위쪽 <M>n</M>은 더할 항의 개수, 아래쪽 <M>{`i=1`}</M>은 시작 인덱스다.
          이걸로 인공 뉴런 식을 다시 적으면 깔끔해진다.
        </p>
        <Mb>{`y \\;=\\; f\\!\\left(\\sum_{i=1}^{n} w_i x_i + b\\right)`}</Mb>
        <p className="text-sm text-muted">
          처음에는 ∑가 어색해 보일 수 있다. 그러나 이건 <strong>새로운 수학이 아니라 그냥 짧게 적는
          약속</strong>일 뿐이다. 앞으로 자주 나오니, 모양만 익숙해져도 충분하다.
        </p>
      </MathCorner>

      <MathCorner title="ReLU의 그래프와 두 갈래로 나뉘는 함수">
        <p>
          ReLU는 짧은 식이지만, 이미 알고 있는 형식으로 다시 쓰면 <strong>경우로 나눈 함수</strong>다.
          중3 때 본 절댓값 함수와 비슷한 모양이다.
        </p>
        <Mb>{`\\mathrm{ReLU}(z) \\;=\\; \\begin{cases} z & (z \\ge 0) \\\\ 0 & (z < 0) \\end{cases}`}</Mb>
        <p>
          그래프로 그리면 <M>z</M>가 양수일 때는 <M>{`y = z`}</M>인 직선, 음수일 때는 평평한
          직선(<M>{`y = 0`}</M>)이 만나 한 점에서 꺾이는 모양이 된다. 이 작은 꺾임 한 번이
          신경망에 비선형성을 만들어 낸다.
        </p>
        <p>
          "비선형"이라는 말이 어렵게 들리겠지만, 그냥 <strong>"직선이 아닌 모양"</strong>이라고
          생각하면 된다. 일차함수로는 곡선을 못 그리지만, 꺾인 직선을 여러 개 모으면 곡선
          비슷한 모양도 만들 수 있다. 신경망은 그 작은 꺾임들을 잔뜩 모아 복잡한 패턴을 그린다.
        </p>
      </MathCorner>

      <KeyTakeaways
        items={[
          '인공 뉴런 한 개는 가중치(w) · 편향(b) · 활성화 함수(f)의 세 부품으로 이뤄진다.',
          '가중치는 "그 입력을 얼마나 중요하게 볼지"를, 편향은 "기본 점수"를, 활성화 함수는 "신호를 낼지 말지"를 정한다.',
          'ReLU는 음수면 0, 양수면 그대로 통과시키는 가장 단순한 활성화 함수다.',
          '입력이 몇 개든 인공 뉴런은 y = f(∑ wᵢxᵢ + b) 한 줄로 적을 수 있다.',
        ]}
      />
    </>
  );
}
