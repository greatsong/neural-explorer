// 5장 — 오차 역전파 (자동 학습) (고1 친화 톤)
import { Aside, Capture, CoreInsights, KeyTakeaways, LabBox, LabSteps, LabQuestions, LabTip, LearningGoals, MathCorner, M, Mb, TrainingCycleStrip } from '../components';

export function TextbookP5() {
  return (
    <>
      <p className="lead text-lg leading-8 text-text/90">
        4장까지 우리는 산을 내려가는 작전을 모두 갖췄다. 어디로 가야 하는지(기울기), 한 발이 얼마나 큰지(학습률).
        그런데 한 가지가 남았다. <strong>"그 한 발을 컴퓨터가 자동으로 옮기는 일"</strong> 말이다. 이 장의 주인공이
        바로 그 자동화의 알고리즘, <strong>오차 역전파(backpropagation)</strong>다.
      </p>
      <p>
        이름은 거창하지만 본질은 단순하다. <strong>오차를 측정하고, 그 오차로부터 변화량을 계산하고, 가중치와 편향을
        그만큼 수정한다.</strong> 이 세 단계의 한 묶음을 한 step이라 한다. 한 step씩 수백 번 반복하면 손실은
        스르륵 0에 가까워진다. "학습"이라는 신비한 말이 이 한 사이클의 반복일 뿐이다.
      </p>

      <LearningGoals
        items={[
          '한 step의 학습이 "오차 측정 → 변화량 계산(dw, db) → 가중치·편향 수정"의 세 단계로 이뤄진다는 것을 자기 말로 설명할 수 있다.',
          '한 점이 가중치 w에 분담시키는 책임은 e × x, 편향 b에 분담시키는 책임은 e인 까닭을 한 줄로 말할 수 있다.',
          '시뮬레이터에서 다섯 점을 자동 학습시키는 모습을 직접 step 단위로 따라가며, dw·db가 어떻게 평균으로 만들어지는지 손으로 확인할 수 있다.',
        ]}
      />

      <CoreInsights
        items={[
          '한 step의 학습은 "오차 측정 → 변화량 계산(dw, db) → 가중치·편향 수정" 세 단계의 한 사이클이다. 이 사이클을 수백 번 반복하는 일이 곧 학습이다.',
          '한 점이 가중치에 분담시키는 책임은 "오차 × 입력(e · x)"이다. 입력 x가 큰 점일수록 가중치가 살짝만 흔들려도 출력이 크게 흔들리니, 그 점의 책임이 더 무겁다.',
          '편향 b는 어느 점에서나 같은 무게로 연결된 "기본 점수"라, 한 점이 분담시키는 책임은 그냥 오차 e 자체다 — x가 곱해지지 않는다.',
        ]}
      />

      <TrainingCycleStrip
        active={[4, 6]}
        caption="이 장은 ④와 ⑥이 한 묶음으로 자동화되는 순간이다. 사람 손 없이 컴퓨터가 ④ 기울기를 직접 계산해 ⑥ w를 갱신한다 — 이 자동 흐름이 곧 역전파."
      />

      <h2>5.1 한 step에 일어나는 세 가지</h2>
      <p>
        시뮬레이터의 <strong>"한 단계 진행"</strong> 버튼을 한 번 누른다고 하자. 그 한 번의 클릭 동안
        컴퓨터 안에서는 정확히 세 가지가 순서대로 일어난다.
      </p>
      <ol className="list-decimal pl-6 my-3 leading-7 space-y-1.5">
        <li>
          <strong>오차 측정</strong> — 다섯 점 모두에서 예측값 <M>{`\\hat{y} = \\mathrm{ReLU}(w x + b)`}</M>를 계산하고,
          정답 <M>y</M>와 비교해 오차 <M>{`e = \\hat{y} - y`}</M>를 구한다.
        </li>
        <li>
          <strong>변화량 계산</strong> — 다섯 점의 오차로부터 두 개의 평균을 만든다.
          <M>{`dw = \\overline{e \\cdot x}`}</M> (오차에 입력을 곱한 것의 평균),
          <M>{`db = \\overline{e}`}</M> (오차의 평균).
        </li>
        <li>
          <strong>가중치·편향 수정</strong> — 4장에서 배운 그 학습률 η를 곱해 한 발씩 옮긴다.
          <M>{`w \\leftarrow w - \\eta \\cdot dw`}</M>, <M>{`b \\leftarrow b - \\eta \\cdot db`}</M>.
        </li>
      </ol>
      <p>
        이게 끝이다. 이 세 단계의 한 묶음이 한 step. 그리고 학습이라는 신비한 단어는 <strong>이 한 묶음을 손실이
        충분히 작아질 때까지 반복하는 일</strong> 그 이상도 이하도 아니다.
      </p>

      <Aside kind="key" title="이 장의 결론을 미리">
        <Mb>{`w \\leftarrow w - \\eta \\cdot \\overline{e \\cdot x}, \\qquad b \\leftarrow b - \\eta \\cdot \\overline{e}`}</Mb>
        이 두 줄짜리 갱신 식이 신경망 학습의 심장이다. 신경망이 100층이든 1000층이든, 결국 각 층의 모든
        가중치·편향에 이 모양의 식이 한 번씩 적용된다.
      </Aside>

      <h2>5.2 책임 분담 — 왜 가중치는 e × x이고 편향은 e인가</h2>
      <p>
        한 점이 만든 오차 <M>e</M>는 누구의 잘못일까. <M>w</M>의 잘못일 수도 있고 <M>b</M>의 잘못일 수도 있다.
        그런데 책임의 무게가 둘이 다르다. 왜 그럴까?
      </p>
      <p>
        예측 식을 다시 보자. <M>{`\\hat{y} = w \\cdot x + b`}</M>(ReLU가 통과시키는 양수 영역에서). 이 식에서
        <M>w</M>를 1만큼 살짝 바꿨다고 하자. 그러면 <M>{`\\hat{y}`}</M>는 <M>x</M>만큼 변한다 — <M>w</M>에 곱해진 게 <M>x</M>니까.
        그래서 입력 <M>x</M>가 큰 점에서는 <M>w</M>가 살짝만 흔들려도 출력이 크게 흔들린다. <strong>그 점의 책임이 더 무겁다</strong>.
      </p>
      <p>
        반면 <M>b</M>는? <M>b</M>를 1만큼 바꾸면 <M>{`\\hat{y}`}</M>도 1만큼 변한다 — 어느 점에서든 똑같이.
        <M>b</M>는 모든 점에 동일한 무게로 연결돼 있다. 그래서 <strong>한 점이 b에 분담시키는 책임은 그냥 오차 e 자체</strong>다.
        x가 곱해지지 않는다.
      </p>
      <p>
        이 한 줄짜리 직관이 곧 갱신 식의 모양이다.
      </p>
      <Mb>{`w\\text{ 책임(한 점)} \\;=\\; e \\cdot x, \\qquad b\\text{ 책임(한 점)} \\;=\\; e`}</Mb>
      <p>
        다섯 점의 책임을 평균 내면 그게 <M>dw</M>와 <M>db</M>다. 자세한 한 줄씩 유도는 시뮬레이터의 "수식 유도" 탭(11단계)에서 따라가 볼 수 있다.
      </p>

      <h2>5.3 다섯 점으로 손계산 — w = 0, b = 0에서 시작하면</h2>
      <p>
        시뮬레이터의 다섯 점은 (1, 3) · (2, 5) · (3, 7) · (4, 9) · (5, 11)이다. 정답은 <M>{`w = 2, b = 1`}</M> — 그러면
        모든 점에서 <M>{`y = 2x + 1`}</M>이 정확히 맞는다. 그런데 학습은 <M>{`w = 0, b = 0`}</M>에서 시작한다. 첫 step에 무슨 일이 일어날까.
      </p>
      <p>
        먼저 다섯 점에서 예측과 오차를 손으로 적어 본다. <M>{`\\hat{y} = \\mathrm{ReLU}(0 \\cdot x + 0) = 0`}</M>이니
        모든 점에서 예측은 0. 오차 <M>{`e = \\hat{y} - y = 0 - y = -y`}</M>다.
      </p>
      <ul className="list-disc pl-6 my-3 leading-7 text-[0.95em]">
        <li><M>{`x=1, y=3 \\;\\Rightarrow\\; e = -3, \\; e \\cdot x = -3`}</M></li>
        <li><M>{`x=2, y=5 \\;\\Rightarrow\\; e = -5, \\; e \\cdot x = -10`}</M></li>
        <li><M>{`x=3, y=7 \\;\\Rightarrow\\; e = -7, \\; e \\cdot x = -21`}</M></li>
        <li><M>{`x=4, y=9 \\;\\Rightarrow\\; e = -9, \\; e \\cdot x = -36`}</M></li>
        <li><M>{`x=5, y=11 \\;\\Rightarrow\\; e = -11, \\; e \\cdot x = -55`}</M></li>
      </ul>
      <p>
        다섯 점의 평균을 내면 <M>{`dw = (-3 - 10 - 21 - 36 - 55) / 5 = -125/5 = -25`}</M>,
        <M>{`db = (-3 - 5 - 7 - 9 - 11) / 5 = -35/5 = -7`}</M>이다. 둘 다 음수다. 음수의 반대 방향으로 가니
        <M>w</M>도 <M>b</M>도 <strong>위로 올라간다</strong>. 정답 <M>{`w = 2, b = 1`}</M>이 위에 있으니 방향이 맞다.
      </p>
      <p>
        학습률 <M>{`\\eta = 0.1`}</M>이라 하자. 한 step 뒤에는
        <M>{`w \\leftarrow 0 - 0.1 \\cdot (-25) = 2.5`}</M>,
        <M>{`b \\leftarrow 0 - 0.1 \\cdot (-7) = 0.7`}</M>이 된다. 단 한 번의 step으로 정답 근처에 와 버렸다.
        다음 step부터는 자잘한 미세 조정이 이어진다. 손계산으로 시뮬레이터의 첫 step을 그대로 따라잡은 셈이다.
      </p>

      <Aside kind="note" title="왜 첫 step이 이렇게 크게 움직였나">
        출발점 <M>{`(0, 0)`}</M>이 정답 <M>{`(2, 1)`}</M>에서 멀리 떨어져 있어 오차가 크다 — 그래서 <M>{`e \\cdot x`}</M>의 절댓값도 커지고,
        한 step의 변화량도 커진다. 학습이 진행돼 정답에 가까워지면 오차가 작아지고, 자연히 한 step의 변화량도 작아진다.
        곡선이 평평해질수록 발걸음이 작아지는 모양 — 이게 경사하강법의 자연스러운 성질이다.
      </Aside>

      <h2>5.4 자동 학습이 한 step씩 진행되는 모습</h2>
      <p>
        실제 시뮬레이터에서는 매 step마다 작은 다이어그램이 한 번씩 깜빡인다. 오차 신호가 예측 노드 <M>{`\\hat{y}`}</M>에서
        출발해 두 갈래로 갈라진다 — 한 갈래는 가중치 <M>w</M>로, 다른 갈래는 편향 <M>b</M>로 흘러 들어간다.
        붉은색 화살표의 굵기가 그 점이 분담한 책임의 크기다.
      </p>
      <p>
        오른쪽에는 학습 곡선이 step마다 한 점씩 늘어난다. 처음에는 가파르게 내려가다, 정답에 가까워질수록
        곡선이 평평해진다. 이 곡선이 0에 닿으면 학습 종료. 그 한 화면 전체가 곧 신경망 학습 그 자체다.
      </p>

      <LabBox step="실습 5-1" title="다섯 점을 자동으로 학습시키기">
        <p>
          페이지 상단의 <strong>"▶ 실습 앱에서 직접 만져보기"</strong> 버튼으로 인앱 시뮬레이터를 연다.
          이 페이즈는 다섯 개의 탭으로 구성돼 있다 — <strong>1. 직관 → 2. 수식 이해 → 3. 수식 유도 → 4. 전체 종합 → 5. 실생활 적용</strong>.
          순서대로 진행하는 것을 권한다.
        </p>

        <Capture
          src="/textbook/captures/p5.png"
          alt="페이즈 5 오차 역전파 시뮬레이터 화면"
          caption="▲ 왼쪽 다이어그램에서 예측 ŷ로부터 두 갈래의 빨간 화살표가 갈라져 가중치 w와 편향 b로 흘러 들어간다. 오른쪽 학습 곡선이 step마다 한 점씩 쌓인다."
        />

        <LabSteps>
          <li><strong>1. 직관</strong> 탭에서 학습률을 기본값(η = 0.1) 그대로 두고 <strong>"한 단계 진행"</strong>을 한 번 누른다. <M>w</M>·<M>b</M>가 0에서 어디로 옮겨졌는지, 빨간 화살표 굵기가 어느 쪽이 더 굵은지 본다.</li>
          <li>같은 탭의 슬라이더로 <strong>입력 x를 1, 3, 5</strong>로 바꿔 가며 비교한다 — 같은 오차여도 x가 클수록 <M>w</M>로 가는 화살표가 굵어지는 모습을 자기 눈으로 확인한다.</li>
          <li><strong>2. 수식 이해</strong> 탭으로 넘어가, 다섯 점에서 만든 <M>{`e \\cdot x`}</M> 표를 본다. 5.3절의 손계산 결과와 같은지 대조한다.</li>
          <li><strong>"20단계 반복"</strong>을 두세 번 눌러 손실 곡선이 0으로 내려가는 모습을 본다. 100 step 안쪽으로 정답 근처에 도달할 것이다.</li>
          <li>도전 — 학습률 슬라이더를 <strong>0.3 이상</strong>으로 올리고 다시 20단계 반복. 손실 곡선이 위로 솟거나 진동하거나, 심지어 "뉴런 사망(Dying ReLU)" 경고가 뜨는 모습을 본다 (이건 4장에서 다룬 발산 현상의 한 형태).</li>
        </LabSteps>

        <LabQuestions>
          <li>5.3절의 손계산에서 첫 step에 <M>w</M>가 0 → 2.5로, <M>b</M>가 0 → 0.7로 갔다. 정답이 <M>{`w = 2, b = 1`}</M>인데 <M>w</M>는 살짝 지나치고 <M>b</M>는 살짝 못 미쳤다. 왜 이렇게 한쪽은 지나치고 다른 한쪽은 못 미쳤을까?</li>
          <li>x = 5인 점과 x = 1인 점이 만든 오차의 절댓값이 같다고 하자(예: 둘 다 |e| = 2). 이 두 점이 가중치 w에 분담시키는 책임의 크기는 얼마나 차이가 날까?</li>
          <li>만약 데이터의 모든 입력 x가 0이라면, <M>w</M>는 학습이 될까? 식 <M>{`dw = \\overline{e \\cdot x}`}</M>로 답을 추리해 보자.</li>
        </LabQuestions>

        <LabTip>
          시뮬레이터의 "3. 수식 유도" 탭에는 <M>{`w \\leftarrow w - \\eta \\cdot e \\cdot x`}</M>가 어디서 나왔는지 11단계로 한 줄씩 유도해 둔
          본문이 있다. 본문이 너무 빨리 끝났다 싶으면 그 탭이 좋은 보충이다. 미분이라는 단어 대신 "기울기"로 풀어 둬 부담이 적다.
        </LabTip>
      </LabBox>

      <MathCorner title="dw가 e × x인 까닭 — 한 줄짜리 유도">
        <p>
          한 점에서의 손실을 <M>{`L = \\tfrac{1}{2}(\\hat{y} - y)^2 = \\tfrac{1}{2}\\, e^2`}</M>이라 하자.
          (앞에 ½을 붙이는 건 미분할 때 ½ × 2 = 1로 깔끔히 사라지게 하는 약속일 뿐, 최소를 찾는 문제는 그대로다.)
          이 손실이 <M>w</M>에 대해 얼마나 변하는지 — 즉 기울기 <M>{`\\partial L / \\partial w`}</M>를 구하면 된다.
        </p>
        <p>
          중간에 <M>{`\\hat{y} = w x + b`}</M>가 끼어 있다. 그래서 변화의 사슬을 한 번 적용한다.
        </p>
        <Mb>{`\\dfrac{\\partial L}{\\partial w} \\;=\\; \\dfrac{\\partial L}{\\partial \\hat{y}} \\cdot \\dfrac{\\partial \\hat{y}}{\\partial w} \\;=\\; (\\hat{y} - y) \\cdot x \\;=\\; e \\cdot x`}</Mb>
        <p>
          앞 인수 <M>{`(\\hat{y} - y)`}</M>는 <M>{`\\tfrac{1}{2} e^2`}</M>를 <M>{`\\hat{y}`}</M>로 한 번 미분해 나온 결과(½ × 2 × e가 e가 된 것).
          뒷 인수 <M>x</M>는 <M>{`w x + b`}</M>를 <M>w</M>로 미분한 결과. 이 둘을 곱한 게 한 점의 기울기다.
          마찬가지로 <M>b</M> 쪽은 <M>{`\\partial \\hat{y} / \\partial b = 1`}</M>이라 곱셈 항이 사라져 그냥 <M>e</M>가 남는다.
        </p>
        <p>
          다섯 점이 있으니 평균 내면 <M>{`dw = \\overline{e \\cdot x}`}</M>, <M>{`db = \\overline{e}`}</M>. 학습률 η를 곱해 빼 주면 갱신 식이 완성된다.
        </p>
        <p className="text-sm text-muted">
          여기서는 ReLU가 통과시키는 영역(<M>{`z = wx + b > 0`}</M>)을 가정해 ReLU의 기울기를 1로 뒀다. 음수 영역에서는
          ReLU의 기울기가 0이라 학습이 멈춘다 — 시뮬레이터에서 학습률을 너무 크게 올렸을 때 뜨는 "뉴런 사망" 경고가 바로 이 상황이다.
        </p>
      </MathCorner>

      <KeyTakeaways
        items={[
          '한 step의 학습은 "오차 측정 → 변화량 계산(dw, db) → 가중치·편향 수정"의 세 단계로 이뤄진다. 학습은 이 사이클의 반복이다.',
          '한 점이 가중치에 분담시키는 책임은 e · x, 편향에 분담시키는 책임은 e다. 입력 x가 큰 점일수록 가중치를 더 크게 흔든다.',
          '갱신 식은 w ← w − η · 평균(e · x), b ← b − η · 평균(e). 이 두 줄이 신경망 학습의 심장이다.',
          'w = 0, b = 0에서 시작해도 다섯 점의 손계산만으로 첫 step의 결과(w ≈ 2.5, b = 0.7)를 정확히 재현할 수 있다.',
        ]}
      />
    </>
  );
}
