// 2장 — 순전파: 입력에서 예측까지 (고1 친화 톤)
import { Aside, Capture, CoreInsights, KeyTakeaways, LabBox, LabSteps, LabQuestions, LabTip, LearningGoals, MathCorner, M, Mb } from '../components';

export function TextbookP2() {
  return (
    <>
      <p className="lead text-lg leading-8 text-text/90">
        1장에서 인공 뉴런 한 개를 분해해 봤다. 가중치 · 편향 · 활성화 함수, 부품은 세 개뿐이었다.
        그런데 이 뉴런은 <strong>혼자서 무엇을 하고 있는 걸까</strong>? 이 장에서 그 답을 본다.
      </p>
      <p>
        뉴런이 하는 일은 한 줄로 말할 수 있다 — <strong>입력을 받아서 예측을 한 번 내놓는다</strong>.
        이 한 줄짜리 흐름을 우리는 <strong>순전파(forward propagation)</strong>라고 부른다.
        이름은 거창하지만, 게임 캐릭터가 입력 키 → 점프 한 번 하는 것과 같은 한 방향 흐름이다.
      </p>

      <LearningGoals
        items={[
          '순전파의 세 단계(곱셈 → 합산 → 활성화)를 자기 말로 설명하고, 손으로 직접 ŷ을 계산할 수 있다.',
          '입력값과 가중치·편향이 주어졌을 때, 합 z가 음수면 ReLU 때문에 ŷ이 0이 된다는 점을 손으로 확인할 수 있다.',
          '뉴런이 한 개에서 여러 개, 한 층에서 여러 층으로 늘어나도 같은 절차가 반복될 뿐임을 설명할 수 있다.',
        ]}
      />

      <CoreInsights
        items={[
          '순전파는 결국 "한 방향으로만 흐르는 계산 줄"이다. 입력에 가중치를 곱하고(곱셈), 모두 더해 편향을 보태고(합산), 활성화 함수에 통과시킨다(활성화). 이 세 단계가 끝나면 예측값 ŷ이 나온다.',
          '예측값 ŷ(와이 햇)과 정답값 y는 다른 글자다. ŷ은 뉴런이 짐작한 답, y는 실제 정답이다. 둘이 어긋난 만큼이 다음 장에서 다룰 "오차"이다.',
          '뉴런이 두 개든 천 개든 새 규칙은 없다. 같은 곱셈·합산·활성화가 옆으로(같은 층에서 동시에) 또 뒤로(다음 층으로 이어서) 반복될 뿐이다.',
        ]}
      />

      <h2>2.1 순전파는 "한 방향으로 흐르는 계산"이다</h2>
      <p>
        순전파라는 말은 한자로 <em>"앞으로(順) 전(傳)한다"</em>는 뜻이다. 입력 쪽에서 출력 쪽으로
        신호가 한 번만, 거꾸로 돌아오지 않고 흘러간다는 약속이다. 도미노가 한 줄로 쓰러지는 모습을
        떠올리면 된다.
      </p>
      <p>
        뉴런 한 개에서 순전파는 이미 1장에서 본 그 절차다.
      </p>
      <ol className="list-decimal pl-6 my-3 leading-7 space-y-1">
        <li><strong>곱셈</strong> — 입력 <M>{`x_i`}</M>마다 가중치 <M>{`w_i`}</M>를 곱한다.</li>
        <li><strong>합산</strong> — 곱한 값을 모두 더하고 편향 <M>b</M>를 보탠다. 이 결과를 <M>z</M>라 부른다.</li>
        <li><strong>활성화</strong> — <M>z</M>를 활성화 함수에 통과시킨다. 그 결과가 예측값 <M>{`\\hat y`}</M>이다.</li>
      </ol>
      <Mb>{`z = \\sum_{i} w_i x_i + b, \\qquad \\hat y = f(z)`}</Mb>
      <p>
        이 책에서는 <M>f</M>로 ReLU를 쓴다. 그러니까 <M>z</M>가 음수면 <M>{`\\hat y = 0`}</M>,
        양수면 <M>{`\\hat y = z`}</M>가 된다. 끝이다.
      </p>

      <Aside kind="key" title="ŷ과 y는 다른 글자다">
        <M>{`\\hat y`}</M>(와이 햇)은 뉴런이 <strong>예측한 값</strong>이고, 그냥 <M>y</M>는 <strong>실제 정답</strong>이다.
        시험 문제에 비유하면 ŷ은 내가 적은 답, y는 답안지의 정답이다. 둘 사이의 거리가 다음 장에서
        다룰 "오차"의 크기다.
      </Aside>

      <h2>2.2 손으로 직접 따라가 보기 — 양수 예제</h2>
      <p>
        말로만 보면 잘 안 와닿는다. 입력 두 개짜리 뉴런으로 한 번 굴려 보자.
        <M>{`x_1 = 2,\\ x_2 = 3,\\ w_1 = 1,\\ w_2 = 2,\\ b = -1`}</M>로 두자.
      </p>
      <p>1단계 — 입력마다 가중치를 곱한다.</p>
      <Mb>{`w_1 x_1 = 1 \\cdot 2 = 2, \\qquad w_2 x_2 = 2 \\cdot 3 = 6`}</Mb>
      <p>2단계 — 모두 더하고 편향을 보탠다.</p>
      <Mb>{`z = 2 + 6 + (-1) = 7`}</Mb>
      <p>3단계 — ReLU에 넣는다. 7은 양수니까 그대로 통과.</p>
      <Mb>{`\\hat y = \\mathrm{ReLU}(7) = 7`}</Mb>
      <p>
        끝이다. 머리로 외우지 말고, <strong>곱셈 먼저 다 끝내고 → 마지막에 한꺼번에 더하기</strong>를
        손에 익히면 된다. 동시에 하려고 하면 부호 실수가 잦다.
      </p>

      <h2>2.3 합이 음수가 되면? — ReLU가 입을 다무는 순간</h2>
      <p>
        이번에는 가중치가 음수인 경우를 보자.
        <M>{`x_1 = 5,\\ x_2 = 2,\\ w_1 = -2,\\ w_2 = -1,\\ b = 3`}</M>이라 하자.
      </p>
      <Mb>{`z = (-2) \\cdot 5 + (-1) \\cdot 2 + 3 = -10 - 2 + 3 = -9`}</Mb>
      <p>
        합이 <M>{`z = -9`}</M>로 음수가 됐다. ReLU에 통과시키면?
      </p>
      <Mb>{`\\hat y = \\mathrm{ReLU}(-9) = 0`}</Mb>
      <p>
        <strong>이 뉴런은 이번 입력에 0을 답했다</strong>. 0이라고 해서 망가진 게 아니라, "이 신호는
        내 관심 밖이다"라고 명확하게 거절한 것이다. 게임에서 보면 캐릭터가 "지금은 안 움직일래"
        하고 가만히 있는 셈이다.
      </p>
      <p>
        신경망이 깊어지면 어떤 뉴런은 거의 항상 깨어 있고, 어떤 뉴런은 특정 입력에만 반응한다.
        이 역할 분담이 곧 <strong>학습의 결과</strong>다. 5장에서 다시 만난다.
      </p>

      <h2>2.4 뉴런이 여러 개여도 규칙은 그대로다</h2>
      <p>
        실제 신경망은 뉴런이 한 줄, 두 줄, 여러 줄로 쌓여 있다. 뉴런이 늘어났다고 새 규칙이 생긴 건
        아니다. <strong>같은 곱셈 → 합산 → 활성화가 동시에·이어서 반복될 뿐이다</strong>.
      </p>
      <ul className="list-disc pl-6 my-3 leading-7 space-y-1">
        <li><strong>옆으로(같은 층)</strong> — 같은 입력을 받은 뉴런 여러 개가 각자의 가중치·편향으로 자기만의 답을 동시에 만든다. 응답은 뉴런마다 다르다.</li>
        <li><strong>뒤로(다음 층)</strong> — 앞 층의 출력 <M>h</M>가 다음 층 뉴런의 입력이 된다. 곱셈 → 합산 → 활성화를 한 번 더 돈다.</li>
      </ul>
      <p>
        예를 들어 입력 3개 → 은닉 뉴런 2개 → 출력 1개로 된 작은 신경망이라 해도, 우리가 할 일은
        <strong>은닉 뉴런 두 개를 따로 계산해 <M>{`h_1, h_2`}</M>를 구하고 → 그 두 값을 출력 뉴런의
        입력으로 다시 쓰는 것</strong>뿐이다. 한 줄짜리 식을 두 번 돌릴 뿐이다.
      </p>

      <Aside kind="note" title={'왜 "순"전파일까'}>
        한 방향으로만 흐른다는 뜻이다. 출력에서 입력으로 거꾸로 돌아오는 흐름은 따로 있다 — 그게
        5장의 <strong>역전파(backpropagation)</strong>다. 학습할 때 정답과 비교한 오차를 거꾸로
        흘려 보내는 과정이다. 이 장에서는 일단 "앞으로 가는 길"만 다룬다.
      </Aside>

      <h2>2.5 한 줄로 정리</h2>
      <p>
        순전파를 한 줄로 적으면 1장 마지막에 본 식과 똑같다.
      </p>
      <Mb>{`\\hat y = f\\!\\left(\\sum_{i=1}^{n} w_i x_i + b\\right)`}</Mb>
      <p>
        뉴런이 한 개일 땐 이 식 한 번. 뉴런이 한 층에 <M>m</M>개 있으면 같은 식이 <M>m</M>번. 층이
        여러 개면 한 층 끝낸 출력이 다음 층 입력으로. <strong>같은 식을 위치만 바꿔가며 돌리는
        것이 신경망의 순전파</strong>다.
      </p>

      <LabBox step="실습 2-1" title="순전파 퀴즈에서 직접 빈칸 채워 보기">
        <p>
          페이지 상단의 <strong>"▶ 실습 앱에서 직접 만져보기"</strong>를 누르면 순전파 퀴즈가 열린다.
          뉴런 한 개부터 직렬 연결, 작은 신경망(3 × 2 × 1)까지 세 단계로 빈칸(?)을 채워 가는 구성이다.
        </p>

        <Capture
          src="/textbook/captures/p2.png"
          alt="페이즈 2 순전파 퀴즈 화면"
          caption="▲ 다이어그램에 입력·가중치·편향이 표시되고, z와 ŷ 빈칸을 직접 입력해 정답을 확인한다. 화면 위쪽의 1·2·3 탭이 단일 뉴런 → 직렬 → 작은 신경망 순서다."
        />

        <LabSteps>
          <li>탭 <strong>1. 단일 뉴런</strong>에서 시작한다. 주어진 값으로 <M>z</M>를 먼저 계산해 입력하고, 정답이 나오면 다이어그램 위에 z 값이 뜬다. 그다음 ReLU를 통과시킨 <M>{`\\hat y`}</M>을 적는다.</li>
          <li>탭 <strong>2. 뉴런 → 뉴런</strong>은 같은 절차를 두 번 돈다. 앞 뉴런의 출력 <M>h</M>가 곧 뒤 뉴런의 입력이다. 중간에 <M>{`z_1`}</M>이 음수가 되는지, ReLU에 잘리는지 끝까지 부호를 따라가 본다.</li>
          <li>탭 <strong>3. 3 × 2 × 1</strong>은 작은 신경망이다. <M>{`h_1, h_2`}</M>를 따로 구한 다음, 그 둘을 다시 출력 뉴런의 입력으로 넣는다. "옆으로 → 뒤로"의 흐름을 손으로 확인한다.</li>
          <li>막혔다면 두 번 틀린 슬롯 아래에 나타나는 <strong>"단계별 풀이 보기"</strong>를 눌러 식만 슬쩍 본 뒤, 다시 직접 계산해 본다.</li>
        </LabSteps>

        <LabQuestions>
          <li>탭 1에서 가중치 부호만 모두 뒤집는다고 상상해 보자(<M>{`w_1, w_2`}</M>를 각각 음수로). 같은 입력에 대해 <M>z</M>는 어떻게 변할까? <M>{`\\hat y`}</M>은? 한 줄로 자기 말로 적어 보자.</li>
          <li>탭 2에서 첫 뉴런의 결과 <M>h</M>가 0이 나왔다고 하자. 뒤 뉴런의 출력 <M>{`\\hat y`}</M>은 그래도 0이 아닐 수 있을까? 왜 그런가?</li>
          <li>탭 3의 <M>{`h_1, h_2`}</M>는 같은 입력 3개를 받지만 서로 다른 값이 나온다. 이유는 무엇인가? 어떤 부품이 그 차이를 만드는가?</li>
          <li>"순전파는 한 방향으로만 흐른다"는 말의 뜻을, 게임이나 도미노 같은 일상의 비유로 한 줄 설명해 보자.</li>
        </LabQuestions>

        <LabTip>
          부호가 섞인 문제는 <strong>곱셈을 모두 끝낸 뒤 한꺼번에 더하기</strong>가 안전하다.
          머릿속에서 곱셈·덧셈을 동시에 굴리면 부호 한두 개를 놓치기 쉽다.
        </LabTip>
      </LabBox>

      <MathCorner title="시그마(∑)로 짧게 적기 — 한 뉴런에서 한 층으로">
        <p>
          입력이 두 개일 때는 손으로 다 적어도 된다. 하지만 입력이 100개라면? 시그마(∑)가 그래서
          나왔다. <em>"같은 모양의 항을 인덱스만 바꿔 가며 모두 더해라"</em>는 약속일 뿐이다.
        </p>
        <Mb>{`\\sum_{i=1}^{n} w_i x_i = w_1 x_1 + w_2 x_2 + \\dots + w_n x_n`}</Mb>
        <p>
          이걸 쓰면 뉴런 한 개의 순전파가 아래 한 줄로 깔끔해진다.
        </p>
        <Mb>{`\\hat y = f\\!\\left(\\sum_{i=1}^{n} w_i x_i + b\\right)`}</Mb>
        <p>
          한 층에 뉴런이 <M>m</M>개 있으면, 뉴런마다 자기 가중치 <M>{`(w_{j1}, w_{j2}, \\dots, w_{jn})`}</M>과
          편향 <M>{`b_j`}</M>를 가진다. <M>j</M>는 뉴런 번호다.
        </p>
        <Mb>{`\\hat y_j = f\\!\\left(\\sum_{i=1}^{n} w_{j i}\\, x_i + b_j\\right) \\quad (j = 1, \\dots, m)`}</Mb>
        <p>
          글자(<M>j</M>, <M>i</M>) 두 개로 늘어나서 어려워 보일 수 있다. 그런데 <strong>한 뉴런의
          식을 뉴런 개수만큼 반복</strong>한 것일 뿐이다. 컴퓨터는 이 반복을 행렬 곱셈 한 번으로
          압축해 빠르게 처리하지만, 뜻은 똑같다.
        </p>
        <p className="text-sm text-muted">
          시그마는 새 수학이 아니다. 짧게 적기 위한 약속이다. 자주 나오니 모양에만 익숙해져도
          충분하다.
        </p>
      </MathCorner>

      <KeyTakeaways
        items={[
          '순전파는 입력에서 출력까지 신호를 한 방향으로만 흘려보내는 과정이다.',
          '뉴런 한 개의 순전파는 곱셈 → 합산 → 활성화의 세 단계뿐이며, ŷ = f(∑ wᵢxᵢ + b) 한 줄로 적는다.',
          'ReLU 때문에 합 z가 음수면 ŷ은 0이 된다. 0은 망가진 게 아니라 "이 신호는 내 관심 밖"이라는 거절이다.',
          '뉴런이 여러 개·여러 층이어도 새 규칙은 없다. 같은 세 단계가 옆으로(동시에), 뒤로(다음 층으로) 반복될 뿐이다.',
        ]}
      />
    </>
  );
}
